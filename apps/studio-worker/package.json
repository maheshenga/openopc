{
  "name": "@kortix/studio-worker",
  "version": "1.0.0",
  "description": "Independent leased worker for Kortix Studio jobs.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "@kortix/api-contract": "workspace:*",
    "@kortix/db": "workspace:*",
    "@kortix/studio-runtime": "workspace:*",
    "postgres": "^3.4.8",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^5.4.0"
  }
}
