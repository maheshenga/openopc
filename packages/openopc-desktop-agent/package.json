{
  "name": "@openopc/desktop-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:sidecar": "bun build src/sidecar-entry.ts --target=node --format=cjs --outfile=dist/openopc-agent-sidecar.cjs",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "agent-tunnel": "workspace:@kortix/agent-tunnel@*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.4.0"
  }
}
